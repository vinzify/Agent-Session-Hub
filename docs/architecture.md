# Architecture

- `src/Common`: shared path and formatting helpers
- `src/Metadata`: alias and config persistence
- `src/Sessions`: Codex session discovery and parsing
- `src/Fzf`: browser rows, preview, and `fzf` integration
- `src/Runtime`: Codex execution and shell wiring
- `src/Commands`: CLI dispatch and subcommand logic
